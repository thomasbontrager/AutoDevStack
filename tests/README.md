# Tests

This directory contains the test suite for AutoDevStack.

## Running Tests

```bash
npm test
```

## Test Coverage

Tests will be added as the project grows. Planned test areas:

- CLI prompt flow
- Template copying and file transformation
- `package.json` name injection
- `_gitignore` → `.gitignore` rename
- Error handling (missing templates, existing directories)
